#include <stdio.h>
#include <stdlib.h>

 int main (){
  int a = 0, b = 1, c;
  
  printf("Os 100 primeiros numeros da sequencia de fibonacci:\n\n");
   for(int i = 1; i <=100; i++){
   	
   	 printf("%d ", a);
	c = a+b;
	a = b;
	b = c;
	
	if (i % 5 == 0);
	  printf("\n");
	  
   }
	
   system("pause");
 
 return 0;
 
}
